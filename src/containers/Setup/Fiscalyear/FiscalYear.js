import React, { Component } from 'react';

class FiscalYear extends Component {

    state = {
        yearCode,
        year,
        startDate,
        endDate,
        status
    }

    render() {
        return (
            <div>

            </div>
        )
    }
}

export default FiscalYear;